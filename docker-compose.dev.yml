services:
  client:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monitoring_app_dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: ["npm", "run", "start:dev"]
